version: '2'
services:
  springbatch-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/springBatch/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=springBatch
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
